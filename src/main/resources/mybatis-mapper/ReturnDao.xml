<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org/DTD Mapper 3.0/EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.hoon.bookmanagement.dao.ReturnDao">

	<!-- Rent SQL -->
	<insert id="returnDao">
		insert into book_rent(rtid, rtname, rtisbn, rttitle, rtauthor, rtoverdue) values(#{param1},#{param2},#{param3},#{param4},#{param5},#{param6})
	</insert>
</mapper>